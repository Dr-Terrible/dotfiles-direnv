# @FUNCTION: use_go
# @INTERNAL
# @DESCRIPTION:
#
use_go() {
	local version="$( go version | cut -d ' ' -f 3 )"
	printf "${version//go/v}"

	# Exports all the binaries from the Go tool/ and bin/ directory
	PATH_add "$( go env GOTOOLDIR )"
	PATH_add "$( go env GOROOT )/bin"
}
