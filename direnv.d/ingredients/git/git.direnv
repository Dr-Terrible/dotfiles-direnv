# @FUNCTION: use_git
# @INTERNAL
# @DESCRIPTION:
#
use_git() {
	local version="$( git --version | cut -d ' ' -f 3 )"
	printf "v${version}"

	# see https://github.com/keybase/keybase-issues/issues/1712
	export GPG_TTY=$(tty)
}
