# Recipe for an open-source project

require utils flint
require golang anderson

# All the projects must have a $TMPDIR pointing to a local directory
path="${PWD}/tmp"
make_tmpfs "${path}"
path_add TMPDIR "${path}"